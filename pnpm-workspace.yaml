overrides:
  tmp: '>=0.2.6'
  yaml@<2.8.3: '>=2.8.3'

allowBuilds:
  esbuild: true
  sharp: true

# Exempt from pnpm's default 24h minimumReleaseAge quarantine so daisyui bumps
# can land the same day they are published.
minimumReleaseAgeExclude:
  - daisyui
